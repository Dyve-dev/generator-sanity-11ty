<header class="header bg-hero flex flex-col" style="background-image: url({{ metadata.hero.image | urlFor}})">
    <div class="flex flex-col hero flex-grow"> 
      <h1><a href="{{ '/' | url }}">{{ metadata.hero.title }}</a></h1>
      {% if metadata.hero.subtitle %}
      <h2 >{{ metadata.hero.subtitle }}</h2>
      {% endif %}
    </div>
      {# <ul class="nav">
        {%- for nav in collections.nav | reverse -%}
        <li class="nav-item{% if nav.url == page.url %} nav-item-active{% endif %}"><a href="{{ nav.url | url }}">{{ nav.data.navtitle }}</a></li>
        {%- endfor -%}
      </ul> #}
      {% include "navigation.njk" %}
    </header>